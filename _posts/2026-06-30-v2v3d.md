---
title: "[3D Reconstruction for Microscopy] V2V3D: View-to-View Denoised 3D Reconstruction for Light-Field Microscopy"
categories: [PaperReview]
tags: [Microscopy, LightField, 3DReconstruction, Denoising, SelfSupervised]
year: "2025"
venue: "CVPR 2025"
layout: post
mathjax: true
paper: "https://arxiv.org/abs/2504.07853"
---

## Introduction

<blockquote><b>왜 Light Field Microscopy(LFM)인가?</b></blockquote>

<p>
<b>Light field microscopy (LFM) has emerged as an critical technology in a diverse array of biomedical applications,
due to its unparalleled ability to capture high-resolution, three-dimensional microscopic scenes with exceptional
precision and efficiency.</b>
일반 광학현미경은 초점이 맞는 평면 한 장만 기록하므로, 3D를 보려면 z축을 훑으며 여러 장(z-stack)을 찍어야 하고
그만큼 시간 해상도를 잃는다.
</p>

<p>
LFM은 대물렌즈의 상평면에 <b>마이크로렌즈 어레이(MLA)</b>를 끼워, 빛을 각도별로 펼쳐 센서에 떨어뜨린다.
<b>The ability of simultaneously recording both spatial and angular information from the sample allows LFM to
generate volumetric data, which is particularly useful in dynamic biological processes where depth and temporal
resolution are both crucial.</b>
즉, 단 한 번의 노출로 공간 + 각도 정보를 함께 잡아 <mark>scanning 없이 3D를 복원</mark>할 수 있다.
</p>

<figure>
  <img src="/assets/paper_img/v2v3d/fig1.png" alt="Background and concept of V2V3D" style="max-width:100%; border-radius:12px;">
  <figcaption style="font-size:14px; color:#57606a; margin-top:8px;">
    Figure 1. (a) 기존 방법은 모든 view를 그대로 학습에 쓰고 physical prior가 없다 → real-world noisy 장면에서 artifact·blur 발생.
    (b) V2V3D는 noisy view를 겹치지 않는 두 부분집합으로 나눠 두 네트워크가 각자 volume을 만들고, PSF prior로 feature를 정렬한다.
  </figcaption>
</figure>

## Preliminary

<blockquote><b>LFM 결상 모델과 PSF — 논문 표기 그대로 최소한만</b></blockquote>

<p>
LFM에서 센서가 측정하는 light field image(LFI)는, 3D 물체 $I$ 의 각 깊이 슬라이스가
<b>PSF(point spread function)</b> 와 합성곱된 뒤 깊이 방향으로 모두 더해진 형태로 모델링된다 (논문 Eq.&nbsp;1):
</p>

<div style="overflow-x:auto;">
$$ \mathrm{LFI}_{u,x,y} \;=\; \sum_{z} \big( I_{x,y,z} \,*\, \mathrm{PSF}_{u,x,y,z} \big) $$
</div>

<ul>
  <li>$\mathrm{LFI}_{u,x,y}$ : $u$번째 view(각도)에서 위치 $(x,y)$의 측정 영상</li>
  <li>$I_{x,y,z}$ : 복원 대상인 3D intensity 볼륨</li>
  <li>$\mathrm{PSF}_{u,x,y,z}$ : <b>view $u$ · 깊이 $z$ 마다 다른</b> point spread function ($*$는 2D convolution)</li>
</ul>

<p>
여기서 핵심은 <b>PSF가 점광원 하나가 시스템을 거쳐 센서에 어떻게 퍼지는지</b>를 나타내는 함수라는 점이다.
LFM은 MLA 때문에 이 PSF가 view와 깊이에 따라 크게 달라지며, 파동광학(wave optics)으로 사전 계산된다.
결국 재구성(reconstruction)이란 측정된 $\mathrm{LFI}$ 로부터 볼륨 $I$ 를 되찾는 <mark>역문제(inverse problem)</mark>다.
이 PSF는 뒤의 feature alignment에서 다시 <b>physical prior</b>로 쓰인다.
</p>

## 기존 LFM 재구성의 한계

<ul>
  <li>
    <b>RLD 계열 (고전)</b> : <b>RLD-based methods rely on a computationally expensive and iterative recovery process.</b>
    위 역문제를 반복적으로 풀어가는데, 매 iteration마다 forward/back projection을 다시 계산해야 하고
    반복이 진행될수록 노이즈가 증폭된다.
  </li>
  <li>
    <b>딥러닝 계열 (VCDNet, DINER 등)</b> : <b>Existing LFM reconstruction algorithms directly utilize all available
    views to guide training.</b> 모든 view를 그대로 supervision에 쓰는데, 이때 <mark>physical prior가 빠져 있어</mark>
    real-world noisy 장면을 복원하면 뚜렷한 artifact와 blur가 생긴다 (Figure 1a).
  </li>
</ul>

## V2V3D: View-to-View 프레임워크

<blockquote><b>Noise2Noise의 아이디어를 LFM의 "view"에 적용한다</b></blockquote>

<p>
<b>We propose a view2view-based simultaneous denoising and 3D reconstruction framework, named V2V3D, which employs
view-to-view paired noisy images (pixel-unmatched) as inputs and outputs.</b>
</p>

<p>
핵심 직관은 Noise2Noise다 — 같은 신호에 <b>서로 독립적인 노이즈</b>가 섞인 두 관측이 있으면, 한쪽으로 다른 쪽을
예측하도록 학습할 때 네트워크는 (예측 불가능한) 노이즈는 버리고 공통 신호만 학습한다. 깨끗한 ground truth가 없어도
denoising이 된다.
</p>

<p>
V2V3D는 전체 $U$개의 view를 <b>겹치지 않는 두 부분집합 $U_1$, $U_2$</b> ($U_1 \cup U_2 = U$, $U_1 \cap U_2 = \varnothing$)로 나눈다.
weight를 공유하는 U-Net $f(\cdot)$ 이 한 부분집합으로 3D 볼륨을 복원하면, 그 볼륨을 다시 <b>반대편</b> 부분집합의
view 각도로 projection 하여 noisy view와 비교한다 (논문 Eq.&nbsp;2–3):
</p>

<div style="overflow-x:auto;">
$$ \widehat{\mathrm{LFI}}_{u_2,x,y} = \sum_z \big( f(\mathrm{LFI}_{U_1,x,y}) * \mathrm{PSF}_{u_2,x,y,z} \big), \qquad
   \widehat{\mathrm{LFI}}_{u_1,x,y} = \sum_z \big( f(\mathrm{LFI}_{U_2,x,y}) * \mathrm{PSF}_{u_1,x,y,z} \big) $$
</div>

<p>
입력 view 집합과 supervision view 집합이 <b>서로 다른 subset</b>이므로, 두 영상은 같은 시점이 아니라
<mark>픽셀이 정렬되지 않는다(pixel-unmatched)</mark>. 그래서 픽셀 복사 같은 trivial한 해(identity mapping)로 빠지지 않고,
구조(structure) 수준에서 신호만 맞춰가며 노이즈를 제거한다. 최종 복원은 두 branch의 볼륨을 평균(fusion)해 얻는다.
</p>

<figure>
  <img src="/assets/paper_img/v2v3d/fig2.png" alt="Overall framework of V2V3D" style="max-width:100%; border-radius:12px;">
  <figcaption style="font-size:14px; color:#57606a; margin-top:8px;">
    Figure 2. V2V3D 전체 구조. 모든 view를 두 subset(odd/even)으로 나누고, 공유 인코더가 feature를 뽑은 뒤
    <b>wave-optics 기반 feature alignment</b>(PSF prior로 만든 kernel과 2D convolution ⊛)로 정렬, 디코더가 volume을 만든다.
    두 subset의 volume은 fusion되며, 각 volume의 projection은 반대편 subset의 view와 similarity loss로 묶인다.
  </figcaption>
</figure>

## Wave-optics 기반 Feature Alignment

<blockquote><b>2D view feature들을 하나의 3D 공간으로 back-projection 하여 모은다</b></blockquote>

<p>
view마다 따로 뽑은 feature를 그냥 합치면, 시점 차이(parallax) 때문에 같은 구조가 서로 다른 위치에 있어 융합이 어렵다.
<b>After extracting features for each view, we apply back-projection to warp all feature maps from their respective
2D spaces into a unified 3D space, facilitating improved feature aggregation.</b>
</p>

<p>
이 back-projection을 위해, 논문은 forward 결상에 쓰던 PSF를 뒤집어 정렬 커널을 만든다 (Eq.&nbsp;5–6):
</p>

<div style="overflow-x:auto;">
$$ \mathrm{PSF}^{-1}_{u,x,y,z} = \mathrm{PSF}_{u,-x,-y,z}, \qquad
   \mathrm{Feature}_{\mathrm{align}} = \mathrm{Feature} * \mathrm{Kernel}_{\mathrm{PSF}^{-1}} $$
</div>

<p>
이때 그냥 PSF로 합성곱하면 feature가 다시 흐려지므로, <b>각 PSF 슬라이스의 무게중심(centroid) 위치만 1, 나머지는 0</b>으로
둔 희소(sparse) 커널을 쓴다. 즉 blur 없이 각 view feature를 올바른 위치로 <b>이동·정렬</b>만 시킨다.
결과적으로 PSF라는 <mark>physical prior</mark>가 네트워크에 직접 주입되어, 통일된 3D 공간에서 view 간 특징이 일관되게 모인다.
</p>

## Loss 설계

<p>
전체 목적함수는 <b>한 subset의 projection ↔ 다른 subset의 noisy view</b>를 맞추는 self-supervised 비교를 기본으로 하되,
기본 MSE에 두 항을 더한다 (논문 Eq.&nbsp;10):
</p>

<div style="overflow-x:auto;">
$$ L_{\mathrm{all}} = L_{\mathrm{MSE}} + \alpha\, L_{\mathrm{FFT}} + \beta\, L_{\mathrm{DC}}
   \qquad (\alpha = 0.1,\; \beta = 1) $$
</div>

<h3>① FFT Loss — 고주파 디테일 복원</h3>
<p>
<b>MSE loss performs well in most scenarios, its effectiveness diminishes significantly in LFM reconstruction due to
optical defocus, resulting in oversmoothing of high-frequency details. Therefore, we adopt the FFT Loss to better
recover high-frequency details.</b>
LFM은 초점이 맞지 않는 빛(defocus)이 섞여, 픽셀 평균오차 MSE만으로 학습하면 결과가 뭉개진다(oversmoothing).
그래서 예측·타깃을 주파수 영역으로 옮겨 비교하는 FFT loss를 함께 둔다 (Eq.&nbsp;8):
</p>

<div style="overflow-x:auto;">
$$ L_{\mathrm{FFT}} = \frac{1}{N}\,\big\| \mathrm{FFT}(\mathrm{LFI}) - \mathrm{FFT}(\widehat{\mathrm{LFI}}) \big\|_2^2 $$
</div>

<h3>② De-crosstalk Loss — 배경 노이즈/crosstalk 억제</h3>
<p>
서로 다른 마이크로렌즈의 빛이 섞이는 crosstalk와 센서 배경 노이즈는 복원 볼륨에 흐릿한 잔상을 남긴다.
<b>We apply the de-crosstalk loss to penalize values that fall below the background noise level of the sensor.</b>
센서 배경 노이즈 세기 $BG$(히스토그램으로 추정)보다 낮은 voxel 값을 ReLU로 끌어내려, 신호가 없어야 할 영역을 정리한다 (Eq.&nbsp;9):
</p>

<div style="overflow-x:auto;">
$$ L_{\mathrm{DC}} = \sum_{x,y,z} \mathrm{ReLU}\big( BG - \hat{I}_{x,y,z} \big) $$
</div>

## Results

<p>
<b>We confirmed the superior performance of V2V3D using both synthetic and real-world data.</b>
synthetic 데이터에서는 정량 지표로, real-world 데이터에서는 시각적 선명도로 RLD·DINER 등 비교군을 앞선다 (Figure 1c의 XZ
단면에서 V2V3D가 GT에 가장 가깝다).
</p>

<p>
공정한 비교를 위해 supervised 기준선도 준비했다.
<b>We retrained VCDNet on our dataset, using noisy LFIs as input and the noise-free 3D signal as supervision.</b>
즉 VCDNet은 noisy 입력 → noise-free 3D라는 <b>깨끗한 정답을 직접 본</b> 강한 supervised 세팅인데,
V2V3D는 그런 정답 없이도(self-supervised) 경쟁력 있는, 혹은 더 나은 복원 품질을 보인다.
</p>

## Conclusion

<p>
V2V3D는 LFM 재구성을 <b>denoising과 3D reconstruction을 동시에</b> 푸는 self-supervised 문제로 재정의했다.
핵심 설계는 세 가지다 — (1) view를 두 subset으로 나눠 <b>view-to-view(pixel-unmatched)</b>로 학습하는 Noise2Noise식 구조,
(2) PSF prior를 이용해 2D feature를 통일된 3D 공간으로 정렬하는 <b>wave-optics 기반 feature alignment</b>,
(3) 고주파를 살리는 <b>FFT loss</b>와 배경을 정리하는 <b>de-crosstalk loss</b>.
깨끗한 ground truth가 귀한 실제 현미경 환경에서, 물리 모델(PSF)과 self-supervision을 결합해
RLD의 느린 반복과 기존 딥러닝의 artifact 문제를 동시에 푼 점이 인상적이다.
</p>
